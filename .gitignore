# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
**/dist/
*.js.map

# Private keys & credentials — NEVER commit
**/ministry/keys/
**/committee/data/
**/ublp-agent/data/
**/l2-verifier-mock/data/

# Transaction audit log (SQLite) — real deal/counterparty data, company-internal only
**/incoterms-escrow/data/

# Logs
*.log

# Environment variables
.env
.env.local
.env.*.local

# SP1 circuit build artifacts
**/sp1-circuit/target/
**/sp1-circuit/elf/

# Compact contract build artifacts (prover/verifier keys, ZKIR — regenerated by `compact compile`)
**/contracts/managed/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Test coverage reports
coverage/
**/coverage/

# Test temp files
**/tmp/

# Internal architecture/decision notes — local only, never public
AGENTS.md

# Local devnet test wallet secrets (encrypted, but throwaway test money only —
# never meant to leave this machine either way)
**/.devnet-secrets/

# levelPrivateStateProvider's on-disk private-state store (local devnet runtime data)
**/midnight-level-db/
